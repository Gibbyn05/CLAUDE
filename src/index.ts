// Remotion entry point — registers all compositions via Root.tsx
export { registerRoot } from "remotion";
import { registerRoot } from "remotion";
import { Root } from "./Root";

registerRoot(Root);
